# Storacle backend – direct dependencies only (pip installs the rest)
# Kept minimal for cross-platform (WSL + Mac)

fastapi>=0.128
uvicorn>=0.40
python-multipart>=0.0.22
python-dotenv>=1.2
google-genai>=0.8
sqlalchemy>=2.0
darts>=0.40
scikit-learn>=1.7
pandas>=3.0
numpy>=2.3

